// automatically generated by Xtext
grammar iDM_Test.StateMachine with org.eclipse.xtext.common.Terminals

import "http://www.example.org/iDM_Test" 
import "http://www.eclipse.org/emf/2002/Ecore" as ecore

StateMachine returns StateMachine:
	{StateMachine}
	'StateMachine'
	name=EString
		(states+=State | transition+=Transition)*
		('init' init = State);

EString returns ecore::EString:
	STRING | ID;

State returns State:
	{State}
	'state' name=EString;

Transition returns Transition:
		'transition' name=EString from=[State|EString]'->' to=[State|EString];